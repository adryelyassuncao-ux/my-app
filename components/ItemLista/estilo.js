import { StyleSheet } from 'react-native';

const estilo = StyleSheet.create({
  boxFilme:{
    padding: 10,
    borderWidth: 1,
    borderColor: '#000',
    borderRadius: 5,
    marginBottom: 10
  },
  nomeFilme: {
    fontSize: 18,
    color: '#204550',
    fontWeight: '600',
    marginBottom: 5
  }
});

export default estilo;
